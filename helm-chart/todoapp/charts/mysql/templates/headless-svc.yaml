apiVersion: v1
kind: Service
metadata:
  name: { { .Chart.Name } }
  namespace: { { .Values.namespace } }
  labels:
    app: { { .Chart.Name } }
spec:
  clusterIP: None
  selector:
    app: { { .Chart.Name } }
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
