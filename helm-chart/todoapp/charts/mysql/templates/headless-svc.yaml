apiVersion: v1
kind: Service
metadata:
  name: { { .Chart.Name } }
  namespace: { { .Values.namespace } }
spec:
  clusterIP: None
  selector: { app: { { .Chart.Name } } }
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
