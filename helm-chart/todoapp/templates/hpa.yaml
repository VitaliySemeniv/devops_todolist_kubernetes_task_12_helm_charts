{{- if .Values.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-hpa
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: {{ include "todoapp.fullname" . }} }
  minReplicas: {{ .Values.hpa.minReplicas }}
  maxReplicas: {{ .Values.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource: { name: cpu,    target: { type: Utilization, averageUtilization: {{ .Values.hpa.averageCPUUtilization }} } }
  - type: Resource
    resource: { name: memory, target: { type: Utilization, averageUtilization: {{ .Values.hpa.averageMemoryUtilization }} } }
{{- end }}
