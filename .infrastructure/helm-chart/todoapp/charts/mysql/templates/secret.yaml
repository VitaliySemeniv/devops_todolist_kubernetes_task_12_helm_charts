apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-auth
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
{{- range .Values.secrets }}
  {{ .name }}: "{{ .value }}"
{{- end }}
