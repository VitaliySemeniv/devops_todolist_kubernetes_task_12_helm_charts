apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default "mysql" .Values.serviceAccount.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    app.kubernetes.io/name: mysql
    app.kubernetes.io/instance: {{ .Release.Name }}
